/**
 * Partner app assets.
 *
 * The Partner app reuses the exact same QuickPress brand imagery as the
 * customer frontend instead of duplicating binaries. Import from here so any
 * future partner-only artwork drops into this folder alongside the re-exports.
 */
import courier from "@/shared/assets/courier-real.png";
import laundryPickup from "@/shared/assets/laundry-pickup.png";
import serviceHero from "@/shared/assets/service-hero.jpg";
import storeFront from "@/shared/assets/store-1.jpg";

export const partnerAssets = {
  courier,
  laundryPickup,
  serviceHero,
  storeFront,
};
